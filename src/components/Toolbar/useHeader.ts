// useHeaderカスタムフックは現時点では必要ありませんが、
// 将来的に状態管理や複雑なロジックが必要になった場合のために
// プレースホルダーとして作成しています。

import { useState } from 'react';

export const useHeader = () => {
  // 将来的な状態管理やロジックをここに追加
  return {};
};
